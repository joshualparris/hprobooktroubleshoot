name: Public evidence guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan public text for recovery-key patterns
        shell: powershell
        run: .\scripts\check-public-evidence.ps1 -Root .
